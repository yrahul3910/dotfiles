local options = {
  ensure_installed = { "lua", "c", "cpp", "vimdoc", "cmake" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
