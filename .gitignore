# Generated inside the repo checkout. App state no longer lands here: stow runs
# with --no-folding, so ~/.config and friends are real directories.
.DS_Store
*.bak
.env
__pycache__/
.ruff_cache/
.venv/
node_modules/
.pi/agent/extensions/pixilate/.env
terraform/**/.terraform/
terraform/**/.terraform.lock.hcl
